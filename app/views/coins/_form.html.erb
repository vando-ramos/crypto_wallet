<%= form_with(model: coin, local: true) do |form| %>
  <% if coin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(coin.errors.count, "error") %> prohibited this coin from being saved:</h2>

      <ul>
      <% coin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :description %>
    <%= form.text_field :description, class:"form-control" %>
  </div>    

  <div class="mb-3">
    <%= form.label :acronym %>
    <%= form.text_field :acronym, class:"form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :url_image %>
    <%= form.text_field :url_image, class:"form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :consensus_mechanism_id %>
    <%= form.select('consensus_mechanism_id', @consensus_mechanism_options, { include_blank: "Select" }, class:"form-control") %>
  </div>

  <div class="actions">
    <%= form.submit nil, class:"btn btn-outline-dark" %>
  </div>
<% end %>

</br>